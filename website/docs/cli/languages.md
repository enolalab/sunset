---
title: languages
---

```bash
sunset languages
```

Prints a table of registered language IDs and file extensions. The current registry is Go (`.go`), JavaScript (`.js`, `.jsx`), TypeScript (`.ts`, `.tsx`), and Python (`.py`).
